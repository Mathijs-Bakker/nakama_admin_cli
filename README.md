# Nakama Admin CLI in Rust

Gebruik dit om RPC functies aan te roepen via Nakama Runtime HTTP API.

## Gebruik
- Vul je `.env` in
- Build met `cargo build --release`
- Run met `./target/release/nakama_admin_cli_rust`

